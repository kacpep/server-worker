`server {
        listen 80; 

        root /var/www/${domain}/public_html;
        
        index index.html index.htm;

        server_name ${domain};

        location / {
           
            try_files $uri $uri/ =404;
        }
    }`